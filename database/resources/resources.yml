# configure tables
- "${file(resources/tables/user_profiles.yml)}"

# configure lambda stream event
- "${file(resources/table-streams.yml)}"

# configure appsync access
- "${file(resources/appsync-access.yml)}"

# autoscaling role
- "${file(resources/autoscale-role.yml)}"
